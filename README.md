# Handwritten-digits-recognition-
Handwritten Digit Recognition using a neural network built with PyTorch and trained on MNIST. The model classifies handwritten digits (0–9) with high accuracy using deep learning, backpropagation, and Adam optimization, with support for training, testing, prediction, and visualization.
